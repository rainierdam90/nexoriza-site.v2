# Allow pnpm install in CI without a strict lockfile match.
# We removed pnpm-lock.yaml deliberately so Vercel regenerates it on each
# fresh install. Once Vercel has built once, you can commit the generated
# lockfile back into the repo for faster, deterministic future builds.
frozen-lockfile=false
